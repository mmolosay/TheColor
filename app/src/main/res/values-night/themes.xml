<resources>

    <style name="Theme.TheColor" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- colors -->
        <item name="colorPrimary">@color/theme_primary</item>
        <item name="colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="colorAccent">@color/theme_accent</item>

        <!-- window -->
        <item name="android:windowBackground">@color/theme_window_background</item>
        <item name="android:statusBarColor">@color/theme_status_bar</item>
        <item name="android:navigationBarColor">@color/theme_navigation_bar</item>

    </style>

</resources>